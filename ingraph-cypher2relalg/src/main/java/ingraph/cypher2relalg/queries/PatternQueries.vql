package ingraph.cypher2relalg.queries

import "http://www.slizaa.org/neo4j/opencypher/OpenCypher"

pattern nodeVariables(variable) {
	NodePattern.variable(_, variable);
}

pattern edgeVariables(variable) {
	RelationshipPattern.detail.variable(_, variable);
}
