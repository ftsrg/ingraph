sudo: required
dist: trusty
language: java
jdk:
  - oraclejdk8
install: false
script:
  - sudo apt-get update
  - sudo apt-get install -y texlive-base texlive-science texlive-fonts-recommended texlive-fonts-extra texlive-latex-extra
  - ./gradlew assemble
  - ./gradlew check -x test --continue
  - cd opencypher-report
  - make
notifications:
  slack: bme-db-lab:egojac3QB9DyjtDvzHiNIaqo
  email: false
  on_success: change
  on_failure: always
