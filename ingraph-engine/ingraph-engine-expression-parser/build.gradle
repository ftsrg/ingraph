plugins {
  id 'com.github.maiflai.scalatest' version '0.14'
}

apply plugin: 'scala'

jar {
  from sourceSets.main.allScala
}

dependencies {
  compile group: 'ingraph-compiler', name: 'ingraph-compiler-cypher2relalg', version: ingraphVersion
  compile group: 'ingraph-compiler', name: 'ingraph-compiler-relalg2rete', version: ingraphVersion

  compile project(':ingraph-engine-ire')

  compile group: 'org.scala-lang', name: 'scala-library', version: scalaVersion
  testCompile group: 'org.scalatest', name: "scalatest_$scalaShortVersion", version: scalaTestVersion
  testRuntime group: 'org.pegdown', name: 'pegdown', version: pegdownVersion
}
