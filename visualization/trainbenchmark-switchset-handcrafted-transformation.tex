\documentclass[varwidth=100cm,convert={density=120}]{standalone}

\input{inputs/relalg-packages}
\input{inputs/relalg-commands}

\begin{document}
\begin{preview}
\begin{tikzpicture}
\Tree
[. {$\duplicateelimination \{\var{semaphore, route, swP, sw, currentPosition, position}\}$}
	[. {$\projection{\var{semaphore},~\var{route},~\var{swP},~\var{sw},~\var{currentPosition},~\var{position}} \{\var{semaphore, route, swP, sw, currentPosition, position}\}$}
		[. {$\selection{\var{sw.currentPosition} \neq \var{swP.position}} \{\var{semaphore, \_e1, route, \_e2, swP, \_e3, sw}\}$}
			[. {$\selection{\var{semaphore.signal} = \literal{"GO"}} \{\var{semaphore, \_e1, route, \_e2, swP, \_e3, sw}\}$}
				[. {$\alldifferent{\var{\_e1},~\var{\_e2},~\var{\_e3}} \{\var{semaphore, \_e1, route, \_e2, swP, \_e3, sw}\}$}
					[. {$\join \{\var{semaphore, \_e1, route, \_e2, swP, \_e3, sw}\}$}
						[. {$\join \{\var{semaphore, \_e1, route, \_e2, swP}\}$}
							[. {$\getedges{semaphore}{Semaphore}{route}{Route}{\_e1}{entry} \{\var{semaphore, \_e1, route}\}$}
							]
							[. {$\getedges{route}{Route}{swP}{SwitchPosition}{\_e2}{follows} \{\var{route, \_e2, swP}\}$}
							]
						]
						[. {$\getedges{swP}{SwitchPosition}{sw}{Switch}{\_e3}{target} \{\var{swP, \_e3, sw}\}$}
						]
					]
				]
			]
		]
	]
]
;
\end{tikzpicture}
\end{preview}
\end{document}
